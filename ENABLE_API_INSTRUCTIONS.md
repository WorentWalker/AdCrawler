# Как включить Places API (New)

## ⚠️ Проблема
Поиск не возвращает результаты из-за ошибки:
```
Places API (New) has not been used in project before or it is disabled
```

## ✅ Решение

### Вариант 1: Прямая ссылка (быстрее всего)

Откройте эту ссылку и нажмите "Enable":
```
https://console.developers.google.com/apis/api/places-backend.googleapis.com/overview?project=82486774539
```

### Вариант 2: Через Google Cloud Console

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите проект (ID: 82486774539)
3. Перейдите в **APIs & Services** → **Library**
4. В поиске введите: **"Places API (New)"**
5. Нажмите на **"Places API (New)"** (НЕ "Places API" без New!)
6. Нажмите кнопку **"Enable"**
7. Подождите 2-3 минуты

### Вариант 3: Через gcloud CLI (если установлен)

```bash
gcloud services enable places-backend.googleapis.com --project=82486774539
```

## ⏱️ Важно!

После включения API подождите **2-3 минуты** перед повторной попыткой поиска.

## 🔍 Проверка

После включения и ожидания, попробуйте:

1. Откройте [http://localhost:3001](http://localhost:3001)
2. Введите:
   - Keywords: `restaurant`
   - Location: `New York, USA`
   - Threshold: `3.0`
3. Нажмите **Search**

Если все правильно, вы увидите результаты!

## ❓ Если не работает

1. **Проверьте биллинг**: Убедитесь, что в проекте включен биллинг (billing)
2. **Проверьте квоты**: В Cloud Console → APIs & Services → Quotas
3. **Проверьте API Key**: Убедитесь, что ключ имеет доступ к Places API (New)
4. **Проверьте ограничения ключа**: В Credentials → ваш API key → убедитесь что Places API (New) разрешен

## 📝 Два разных API

| API | URL | Статус |
|-----|-----|--------|
| Places API (старый) | places.googleapis.com | ❌ Не используется |
| **Places API (New)** | places-backend.googleapis.com | ✅ **Нужен этот!** |

## 🎯 После включения

Приложение автоматически начнет работать. Не нужно перезапускать сервер или менять код.
